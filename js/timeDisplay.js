export function initTimeDisplay(displayElement) {    function setCurrentTime() {        let now = new Date();        let hour = now.getHours();        // console.log(hour);        let min = now.getMinutes();        let displayHour = (hour < 10 ? "0" : "") + hour;        let displayMin = (min < 10 ? "0" : "") + min;        displayElement.innerText = `${displayHour}h${displayMin}`;        return hour;    }    // Initialisation de l'affichage de l'heure    setCurrentTime();    setInterval(setCurrentTime, 2000);    return setCurrentTime;}